As of now this code works with python 3.9.1 and the libraries with specified versions in requirements.txt.

Run in live video mode:

`python sonification_video.py`

At startup in video mode, choose either a window or the camera from the interactive selector.

If you choose camera, you can enter the camera index (default `0`).